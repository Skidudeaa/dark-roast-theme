// Dark Roast: Black Label — Main Entry Point
// Re-exports all design tokens from the generated barrel (dist/tokens).
// Source of truth is src/tokens.json; run `npm run build` to regenerate dist/.

export * from './dist/tokens/index.js';
