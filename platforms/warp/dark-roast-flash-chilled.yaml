# AUTO-GENERATED by scripts/build-variants.js — DO NOT EDIT.
# ============================================================================
# Dark Roast: Flash Chilled — Warp Terminal Theme
# Version: 5.1.0 Companion
#
# INSTALL
# -------
#   1. Copy this file to ~/.warp/themes/dark-roast-flash-chilled.yaml
#      (create ~/.warp/themes/ if it doesn't exist)
#   2. Open Warp → Settings → Appearance → Theme
#   3. Select "dark-roast-flash-chilled" from the dropdown
#
# PALETTE PHILOSOPHY
# ------------------
# All four Dark Roast terminal themes (Warp, Tabby, Terminal.app, iTerm2) share
# an identical ANSI-16 palette so that ls, git diff, vim syntax, tmux, and every
# other CLI tool produce exactly the same output regardless of which emulator
# you're running.
#
# The palette is grounded in the Dark Roast design system:
#   - Background/foreground pulled directly from DR tokens (void, crema)
#   - Cursor matches the editor cursor (teal — "kinetic, live data flow")
#   - Selection uses espresso (#9EBEC4) — warm but not glaring (v4: was `grain`)
#   - ANSI normals and brights use the five DR action colors + three
#     terminal-extension colors (sage, slate, mauve) that aren't in the core
#     UI token set but sit squarely within the warm espresso aesthetic
#
# FULL COLOR REFERENCE
# --------------------
# Chrome:
#   background    #D4E1E3   void      — warm-dark deepest layer
#   foreground    #000000   crema     — primary text (15.68:1 contrast on void)
#   cursor        #006464   teal      — kinetic, matches editor caret
#   cursor text   #D4E1E3   void      — inverted on cursor block
#   selection bg  #9EBEC4   espresso  — warm polished surface (v4: was `grain`)
#   selection fg  #000000   crema     — full brightness on selection
#
# ANSI Normal (indices 0–7):
#   0 black       #C2D5D9   obsidian  — dark but not void; provides depth in dark bg
#   1 red         #B6011C   scarlet   — error / critical
#   2 green       #1C6402   sage      — warm fern green (terminal extension)
#   3 yellow      #7E4F01   amber     — warning / accent / CTA
#   4 blue        #015E8C   slate     — dusty steel blue (terminal extension)
#   5 magenta     #5A129D   mauve     — dried lavender (terminal extension)
#   6 cyan        #006464   teal      — live data flow / resolved
#   7 white       #000C0F   bone      — reduced-contrast light text
#
# ANSI Bright (indices 8–15):
#   8  bright black    #3B5F65   crater-deep   — comment gray / muted metadata (v4: was `crater`)
#   9  bright red      #B6011C   scarlet-lt    — scarlet companion-bright
#  10  bright green    #1C6700   sage-lt       — sage companion-bright
#  11  bright yellow   #725400   gold          — success / stable severity
#  12  bright blue     #015E8C   slate-lt      — slate companion-bright
#  13  bright magenta  #6605B3   mauve-lt      — mauve companion-bright
#  14  bright cyan     #006464   teal-lt       — teal brightened (noticeably lighter
#                                               than normal teal; brights should be
#                                               brighter, not identical)
#  15  bright white    #000000   crema         — full brightness
# ============================================================================

# ── Warp chrome ─────────────────────────────────────────────────────────────
accent:     "#7E4F01"   # amber — selection highlights and Warp UI accent ring
background: "#D4E1E3"   # void — warm-dark deepest layer
foreground: "#000000"   # crema — primary text, 15.68:1 contrast vs void
cursor:     "#006464"   # teal — kinetic live-data cursor, matches editor caret
details:    darker      # Warp lightens or darkens chrome decoration; "darker"
                        # keeps tab bar and sidebar darker than the background,
                        # which is correct for a warm-dark bg

# ── ANSI-16 terminal colors ──────────────────────────────────────────────────
terminal_colors:
  normal:
    black:   "#C2D5D9"  # ANSI 0 — obsidian (elevated floor, not as deep as void)
    red:     "#B6011C"  # ANSI 1 — scarlet (error / critical severity)
    green:   "#1C6402"  # ANSI 2 — sage (warm fern, used by git diff +lines, ls dirs)
    yellow:  "#7E4F01"  # ANSI 3 — amber (warning / accent; matches DR primary CTA)
    blue:    "#015E8C"  # ANSI 4 — slate (dusty steel; git diff header, less prompt)
    magenta: "#5A129D"  # ANSI 5 — mauve (dried lavender; grep matches, vim special)
    cyan:    "#006464"  # ANSI 6 — teal (live data; matches cursor + DR teal token)
    white:   "#000C0F"  # ANSI 7 — bone (reduced-contrast; comment-level brightness)

  bright:
    black:   "#3B5F65"  # ANSI 8  — crater-deep (comment gray; git log graph, tmux inactive)
    red:     "#B6011C"  # ANSI 9  — scarlet companion-bright (diff removed, error paths)
    green:   "#1C6700"  # ANSI 10 — sage companion-bright (diff added, success paths)
    yellow:  "#725400"  # ANSI 11 — gold (success / stable; DR gold token, git branch)
    blue:    "#015E8C"  # ANSI 12 — slate companion-bright (links, prompt path segments)
    magenta: "#6605B3"  # ANSI 13 — mauve companion-bright (vim visual, special tokens)
    cyan:    "#006464"  # ANSI 14 — teal lightened (brighter than normal teal #006464;
                        #            satisfies "bright should look brighter" convention)
    white:   "#000000"  # ANSI 15 — crema (full brightness; bold text, headings)
