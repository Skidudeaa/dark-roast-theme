# AUTO-GENERATED by scripts/build-variants.js — DO NOT EDIT.
# ============================================================================
# Dark Roast: Velvet Noir — Warp Terminal Theme
# Version: 5.1.0 Companion
#
# INSTALL
# -------
#   1. Copy this file to ~/.warp/themes/dark-roast-velvet-noir.yaml
#      (create ~/.warp/themes/ if it doesn't exist)
#   2. Open Warp → Settings → Appearance → Theme
#   3. Select "dark-roast-velvet-noir" from the dropdown
#
# PALETTE PHILOSOPHY
# ------------------
# All four Dark Roast terminal themes (Warp, Tabby, Terminal.app, iTerm2) share
# an identical ANSI-16 palette so that ls, git diff, vim syntax, tmux, and every
# other CLI tool produce exactly the same output regardless of which emulator
# you're running.
#
# The palette is grounded in the Dark Roast design system:
#   - Background/foreground pulled directly from DR tokens (void, crema)
#   - Cursor matches the editor cursor (teal — "kinetic, live data flow")
#   - Selection uses espresso (#3A1528) — warm but not glaring (v4: was `grain`)
#   - ANSI normals and brights use the five DR action colors + three
#     terminal-extension colors (sage, slate, mauve) that aren't in the core
#     UI token set but sit squarely within the warm espresso aesthetic
#
# FULL COLOR REFERENCE
# --------------------
# Chrome:
#   background    #14030C   void      — warm-dark deepest layer
#   foreground    #F6E4DA   crema     — primary text (16.25:1 contrast on void)
#   cursor        #36AFAB   teal      — kinetic, matches editor caret
#   cursor text   #14030C   void      — inverted on cursor block
#   selection bg  #3A1528   espresso  — warm polished surface (v4: was `grain`)
#   selection fg  #F6E4DA   crema     — full brightness on selection
#
# ANSI Normal (indices 0–7):
#   0 black       #1F0514   obsidian  — dark but not void; provides depth in dark bg
#   1 red         #D4445E   scarlet   — error / critical
#   2 green       #51A460   sage      — warm fern green (terminal extension)
#   3 yellow      #E28233   amber     — warning / accent / CTA
#   4 blue        #6486B9   slate     — dusty steel blue (terminal extension)
#   5 magenta     #C85BA0   mauve     — dried lavender (terminal extension)
#   6 cyan        #36AFAB   teal      — live data flow / resolved
#   7 white       #E3CAC0   bone      — reduced-contrast light text
#
# ANSI Bright (indices 8–15):
#   8  bright black    #A88594   crater-deep   — comment gray / muted metadata (v4: was `crater`)
#   9  bright red      #EC6175   scarlet-lt    — scarlet companion-bright
#  10  bright green    #70BB7C   sage-lt       — sage companion-bright
#  11  bright yellow   #E8A72D   gold          — success / stable severity
#  12  bright blue     #7B9ED2   slate-lt      — slate companion-bright
#  13  bright magenta  #E076B7   mauve-lt      — mauve companion-bright
#  14  bright cyan     #59CAC5   teal-lt       — teal brightened (noticeably lighter
#                                               than normal teal; brights should be
#                                               brighter, not identical)
#  15  bright white    #F6E4DA   crema         — full brightness
# ============================================================================

# ── Warp chrome ─────────────────────────────────────────────────────────────
accent:     "#E28233"   # amber — selection highlights and Warp UI accent ring
background: "#14030C"   # void — warm-dark deepest layer
foreground: "#F6E4DA"   # crema — primary text, 16.25:1 contrast vs void
cursor:     "#36AFAB"   # teal — kinetic live-data cursor, matches editor caret
details:    darker      # Warp lightens or darkens chrome decoration; "darker"
                        # keeps tab bar and sidebar darker than the background,
                        # which is correct for a warm-dark bg

# ── ANSI-16 terminal colors ──────────────────────────────────────────────────
terminal_colors:
  normal:
    black:   "#1F0514"  # ANSI 0 — obsidian (elevated floor, not as deep as void)
    red:     "#D4445E"  # ANSI 1 — scarlet (error / critical severity)
    green:   "#51A460"  # ANSI 2 — sage (warm fern, used by git diff +lines, ls dirs)
    yellow:  "#E28233"  # ANSI 3 — amber (warning / accent; matches DR primary CTA)
    blue:    "#6486B9"  # ANSI 4 — slate (dusty steel; git diff header, less prompt)
    magenta: "#C85BA0"  # ANSI 5 — mauve (dried lavender; grep matches, vim special)
    cyan:    "#36AFAB"  # ANSI 6 — teal (live data; matches cursor + DR teal token)
    white:   "#E3CAC0"  # ANSI 7 — bone (reduced-contrast; comment-level brightness)

  bright:
    black:   "#A88594"  # ANSI 8  — crater-deep (comment gray; git log graph, tmux inactive)
    red:     "#EC6175"  # ANSI 9  — scarlet companion-bright (diff removed, error paths)
    green:   "#70BB7C"  # ANSI 10 — sage companion-bright (diff added, success paths)
    yellow:  "#E8A72D"  # ANSI 11 — gold (success / stable; DR gold token, git branch)
    blue:    "#7B9ED2"  # ANSI 12 — slate companion-bright (links, prompt path segments)
    magenta: "#E076B7"  # ANSI 13 — mauve companion-bright (vim visual, special tokens)
    cyan:    "#59CAC5"  # ANSI 14 — teal lightened (brighter than normal teal #36AFAB;
                        #            satisfies "bright should look brighter" convention)
    white:   "#F6E4DA"  # ANSI 15 — crema (full brightness; bold text, headings)
