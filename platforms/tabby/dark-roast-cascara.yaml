# AUTO-GENERATED by scripts/build-variants.js — DO NOT EDIT.
# ============================================================================
# Dark Roast: Cascara — Tabby Terminal Theme
# Version: 5.1.0 Companion
#
# WHAT IS THIS
# ------------
# A complete color scheme for Tabby terminal (https://tabby.sh), implementing
# the Dark Roast design system's ANSI-16 palette. Tabby is cross-platform
# (macOS, Linux, Windows) and uses a YAML config file.
#
# INSTALL — ALL PLATFORMS (macOS / Linux / Windows)
# --------------------------------------------------
# Option A: Merge into your existing config.yaml
#   1. Locate your Tabby config file:
#        macOS:   ~/Library/Application Support/tabby/config.yaml
#        Linux:   ~/.config/tabby/config.yaml
#        Windows: %APPDATA%\tabby\config.yaml
#   2. Under the top-level `terminal:` key, add or merge:
#        - The `customColorSchemes:` block (the scheme definition)
#        - The `colorScheme:` key to activate it
#   3. Save. Tabby picks up changes automatically (no restart needed).
#
# Option B: Use Tabby's built-in config editor
#   1. Open Tabby → Settings → Config File (opens the YAML editor)
#   2. Find or create the `terminal:` key
#   3. Paste this entire file's content, then save
#
# Option C: Start fresh from this file
#   If you don't have a config.yaml yet, this file IS a valid minimal
#   config. Place it at the platform path above.
#
# ACTIVATING THE SCHEME
# ---------------------
# After merging, ensure the `colorScheme` selector is set:
#
#   terminal:
#     colorScheme:
#       name: "Dark Roast: Cascara"
#
# Then in Settings → Appearance → Terminal, background should be set to
# "From color scheme" for void (#E5DBDA) to apply correctly.
#
# PALETTE PHILOSOPHY
# ------------------
# All four Dark Roast terminal themes (Warp, Tabby, Terminal.app, iTerm2)
# share an identical ANSI-16 palette so that ls, git diff, vim syntax,
# tmux, and every other CLI tool produce exactly the same visual output
# regardless of which emulator you're in.
#
# FULL COLOR REFERENCE
# --------------------
# Chrome:
#   foreground      #010000   crema     — primary text (15.46:1 contrast on void)
#   background      #E5DBDA   void      — warm-dark deepest layer
#   cursor          #006262   teal      — kinetic, matches editor caret
#   cursorAccent    #E5DBDA   void      — text color inside cursor block (inverted)
#   selection bg    #C9B1AF66 espresso+40% — warm espresso at ~40% alpha (v4: was `grain`)
#   selectionFg     #010000   crema     — full brightness text on selection
#
# ANSI Normal (indices 0–7):
#   0 black       #DCCDCB   obsidian  — dark floor, provides depth against void
#   1 red         #B4001B   scarlet   — error / critical
#   2 green       #144904   sage      — warm fern (terminal extension)
#   3 yellow      #7D4E00   amber     — warning / accent / CTA
#   4 blue        #015D8A   slate     — dusty steel blue (terminal extension)
#   5 magenta     #4C1D80   mauve     — dried lavender (terminal extension)
#   6 cyan        #006262   teal      — live data flow / resolved
#   7 white       #130504   bone      — reduced-contrast light text
#
# ANSI Bright (indices 8–15):
#   8  bright black    #674E4C   crater-deep — comment gray, tmux inactive (v4: was `crater`)
#   9  bright red      #B4001B   scarlet+  — scarlet companion-bright
#  10  bright green    #195C02   sage+     — sage companion-bright
#  11  bright yellow   #705300   gold      — success / stable severity
#  12  bright blue     #015D8A   slate+    — slate companion-bright
#  13  bright magenta  #480381   mauve+    — mauve companion-bright
#  14  bright cyan     #006262   teal+     — teal brightened (noticeably lighter
#                                            than normal teal; "bright" means
#                                            brighter, not identical)
#  15  bright white    #010000   crema     — full brightness
# ============================================================================

# ── Tabby activation ─────────────────────────────────────────────────────────
# This block tells Tabby which scheme to use at runtime. It must appear
# somewhere in your config.yaml's `terminal:` section.
terminal:
  colorScheme:
    name: "Dark Roast: Cascara"

  # ── Scheme definition ────────────────────────────────────────────────────
  customColorSchemes:
    - name: "Dark Roast: Cascara"

      # Chrome colors
      foreground:          '#010000'    # crema — primary text
      background:          '#E5DBDA'    # void — warm-dark deepest layer
      cursor:              '#006262'    # teal — kinetic cursor, matches editor caret
      cursorAccent:        '#E5DBDA'    # void — text inside cursor block (inverted)
      # Tabby uses CSS color strings; the trailing 66 is hex for ~40% alpha.
      # Espresso (#C9B1AF) at 40% gives a warm visible selection without contrast clash.
      selection:           '#C9B1AF66'  # espresso + 40% alpha — warm polished surface
      selectionForeground: '#010000'    # crema — ensures legibility on selection bg

      # ANSI-16 palette
      # Tabby expects exactly 16 entries in order: 0–7 normal, 8–15 bright.
      # Each entry corresponds to ANSI escape code \e[3Xm (normal) / \e[9Xm (bright).
      colors:
        # ── Normal (ANSI indices 0–7) ────────────────────────────────────
        - '#DCCDCB'    # 0  ANSI Black    — obsidian (dark floor, not as deep as void)
        - '#B4001B'    # 1  ANSI Red      — scarlet (error / critical severity)
        - '#144904'    # 2  ANSI Green    — sage (warm fern; ls dirs, git diff +lines)
        - '#7D4E00'    # 3  ANSI Yellow   — amber (warning / accent; DR primary CTA)
        - '#015D8A'    # 4  ANSI Blue     — slate (dusty steel; git diff headers)
        - '#4C1D80'    # 5  ANSI Magenta  — mauve (dried lavender; grep matches)
        - '#006262'    # 6  ANSI Cyan     — teal (live data; matches cursor token)
        - '#130504'    # 7  ANSI White    — bone (reduced-contrast light text)
        # ── Bright (ANSI indices 8–15) ───────────────────────────────────
        - '#674E4C'    # 8  Bright Black  — crater-deep (comment gray; tmux inactive pane)
        - '#B4001B'    # 9  Bright Red    — scarlet companion-bright (diff removed)
        - '#195C02'    # 10 Bright Green  — sage companion-bright (diff added)
        - '#705300'    # 11 Bright Yellow — gold (success / stable; git branch name)
        - '#015D8A'    # 12 Bright Blue   — slate companion-bright (links, paths)
        - '#480381'    # 13 Bright Magenta — mauve companion-bright (special tokens)
        - '#006262'    # 14 Bright Cyan   — teal lightened (brighter than ANSI 6;
                       #                    "bright" should be visually distinct)
        - '#010000'    # 15 Bright White  — crema (full brightness; bold headings)
